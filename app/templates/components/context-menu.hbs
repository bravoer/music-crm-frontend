{{#click-outside action='closeMenu'}}
  <i class='material-icons' {{action 'openMenu'}}>more_vert</i>
  {{#if showMenu}}
    {{#ember-tether
      target=target
      targetAttachment=targetAttachment
      attachment=attachment
      }}
      {{yield}}
    {{/ember-tether}}
  {{/if}}
{{/click-outside}}
