<div class="row center">
{{#each-in sortedPagination as |key value|}}
  {{#if value}}
    <i class="small material-icons" {{action 'changePage' value}}>{{pagination-icon key}}</i>
  {{else}}
    <i class="small material-icons disabled">{{pagination-icon key}}</i>
  {{/if}}
{{/each-in}}
</div>
