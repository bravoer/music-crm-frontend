<div class="row">
  {{#if archived}}
    {{#if scores}}
      {{md-btn text='Activeer' icon='mdi-content-inbox' action='toggleArchive' isDisabled=selectedScoresIsEmpty}}
    {{/if}}
  {{else}}
    {{#link-to 'admin.scores.add' class="waves-effect waves-light btn"}}<i class="mdi-content-add left"></i> Nieuw{{/link-to}}
    {{#if scores}}
      {{md-btn text='Archiveer' icon='mdi-content-archive' action='toggleArchive' isDisabled=selectedScoresIsEmpty}}
    {{/if}}
  {{/if}}
  {{#if scores}}
    {{md-btn text='Verwijder' icon='mdi-action-delete' action='delete' isDisabled=selectedScoresIsEmpty}}
  {{/if}}
</div>

{{#if scores}}
  <div class="row">
    <div class="col s12">
      <table class="striped">
	<thead>
	  <tr>
	    <th><!-- Checkbox --></th>
	    <th>Titel</th>
	    <th>Componist</th>
	    <th>Arrangeur</th>
	    <th>Genre</th>
	    <th># Partituren</th>
	    <th><!-- Buttons --></th>
	  </tr>
	</thead>

	<tbody>
	  {{#each scores as |score|}}
	    <tr>
	      <td>{{md-check checked=score.isSelected}}</td>
	      <td>{{score.title}}</td>
	      <td>{{score.composer}}</td>
	      <td>{{score.arranger}}</td>
	      <td>{{t-label value=score.genre}}</td>
	      <td>{{score.parts.length}}</td>
	      <td>
		<a {{action 'info' score}}><i class="material-icons">info</i></a>
		{{#link-to 'admin.scores.edit' score}}<i class="material-icons">mode_edit</i>{{/link-to}}
		<a {{action 'attach' score}}><i class="material-icons">attach_file</i></a>

	      </td>
	    </tr>
	  {{/each}}
	</tbody>
      </table>
    </div>
  </div>
{{else}}
  <p>Geen muziekstukken</p>
{{/if}}



