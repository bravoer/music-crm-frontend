{{md-tabs content=tabs selected=activeTab colWidth=6}}

{{#if isActiveScores}}
  {{scores-table scores=activeScores archived=false delete='deleteScores'}}
{{else}}
  {{scores-table scores=archivedScores archived=true delete='deleteScores'}}
{{/if}}

