`import { sortIcon } from '../../../helpers/sort-icon'`
`import { module, test } from 'qunit'`

module 'SortIconHelper'

# Replace this with your real tests.
test 'it works', (assert) ->
  result = sortIcon 42
  assert.ok result
