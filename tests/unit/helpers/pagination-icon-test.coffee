`import { paginationIcon } from '../../../helpers/pagination-icon'`
`import { module, test } from 'qunit'`

module 'PaginationIconHelper'

# Replace this with your real tests.
test 'it works', (assert) ->
  result = paginationIcon 42
  assert.ok result
